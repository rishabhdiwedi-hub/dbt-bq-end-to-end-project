[project]
name = "data-project-dbt"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "rishi1988", email = "rishabh.diwedi@gmail.com" }
]
requires-python = ">=3.14"
dependencies = [
    "airflow-operators>=0.11.0",
    "apache-airflow>=3.3.2",
    "apache-airflow-providers-docker>=4.5.9",
    "dbt-bigquery>=1.12.1",
    "dbt-core>=1.12.5",
]

[project.scripts]
data-project-dbt = "data_project_dbt:main"

[build-system]
requires = ["uv_build>=0.12.0,<0.13.0"]
build-backend = "uv_build"
