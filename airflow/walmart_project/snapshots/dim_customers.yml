snapshots:
  - name: dim_customers
    relation: ref('eph_customers')
    config:
      database: project-a43efffc-4342-40fc-a08
      schema: walmart_gold
      unique_key: customer_id
      strategy: timestamp
      updated_at: customer_updated_timestamp
      dbt_valid_to_current: "TIMESTAMP('9999-12-31 00:00:00+00')" # Specifies that current records should have `dbt_valid_to` set to `'9999-12-31'` instead of `NULL`.